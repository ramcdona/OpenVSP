CMAKE_MINIMUM_REQUIRED( VERSION 3.1 )

SET( PYTEST_BASE_COMMAND "${PYTHON_VENV_EXE} -m pytest" )

MESSAGE( STATUS "PYTEST_BASE_COMMAND = ${PYTEST_BASE_COMMAND}" )

ADD_TEST( PyTest ${PYTHON_VENV_EXE} -m pytest )
